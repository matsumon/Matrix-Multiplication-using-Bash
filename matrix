dim()
{
	if (( 0 == NULL && $# != 1))
	then
		1>&2	
		exit 1
	else
		if (( 0 == NULL ))
		then 
			cat $1 > 0 
		fi
		head -n1 0 > temp
		wc -l < 0 > dims
		wc -c < temp >> dims
		tr -s '\n' ' ' < dims 
	fi
}
if (( $1 == dims))
then
	dim $2
fi
