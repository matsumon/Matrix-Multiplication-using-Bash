dims()
{
	cat $1 > full
	head -n1 < full > temp
	wc -l < full > dims
	wc -w < temp >> dims
	tr -s '\n' ' ' < dims 
	rm -rf full temp dims
	exit '0'
}
transpose()
{
	cat $1 > full
	cut -f 1 full  
	
}
if [ "$1" == "dims" ]
then
	dims $2
	
elif [ "$1" == "transpose" ]
then
	transpose $2
fi
